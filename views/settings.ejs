<div class="p-4">
  <%- include('./partials/messages') %>
  <h1 class="font-bold text-lg">Settings</h1>
  <%- include('./partials/settings/composer_api') %> <%-
  include('./partials/settings/parameters') %> <%-
  include('./partials/settings/change_password') %> <% if(typeof users !=
  'undefined' && user.is_admin && users.length > 0) { %><%-
  include('./partials/settings/users') %><% } %>
</div>
